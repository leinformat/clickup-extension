const form = document.querySelector('.clickup-theme-uploader__form');
const formLoader = document.querySelector('.clickup-theme-uploader__loader');
const labelMessage = document.querySelector('.clickup-theme-uploader__message');

export { 
    form,
    formLoader,
    labelMessage
};